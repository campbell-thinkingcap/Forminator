# Notification Rule Collection

Collects the notification rules configured for a branch. Each document in the
collection is one rule: which event triggers it, which channels deliver it,
and whether it is active. Administered from the branch notifications page;
stored per branch.
